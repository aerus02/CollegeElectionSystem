#College Election System



#add a category,view current votes count,add all users at beginning(add one during event if possible) add candidate
#users - login,add a vote for each category,if done ,no update

from election_system import app


if __name__ == '__main__':
    app.run(debug=True)